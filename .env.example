# SPACE_URL: the local model server started with space/app.py.
SPACE_URL="http://127.0.0.1:7860"

# HF_TOKEN: Hugging Face read token with access to phuuun/saysomething-*.
# Only needed the first time, to download the weights.
HF_TOKEN="hf_..."
